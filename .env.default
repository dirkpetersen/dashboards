# Global AWS profile to use for credentials (used by all dashboards)
# AWS_PROFILE=bedrock

# Global subnets to allow connections from (comma-separated CIDR list)
# Only connections from these subnets will be allowed to access the dashboard
# Note: 127.0.0.1/8 (localhost) is always implicitly allowed
# SUBNETS_ONLY=192.168.0.0/16,10.0.0.0/8

# Global FQDN the dashboards will be hosted under
# FQDN=bedrock-usage.example.com

# ============================================================================
# Bedrock Usage Dashboard Specific Configuration
# ============================================================================
# These override the global settings for bedrock-usage.py specifically

# AWS profile for Bedrock Usage dashboard (overrides AWS_PROFILE)
# AWS_PROFILE_BEDROCK_USAGE=bedrock

# Subnet restriction for Bedrock Usage dashboard (overrides SUBNETS_ONLY)
# SUBNETS_ONLY_BEDROCK_USAGE=192.168.0.0/16,10.0.0.0/8

# FQDN for Bedrock Usage dashboard (overrides FQDN)
# FQDN_BEDROCK_USAGE=bedrock-usage.example.com
